##MEMBER BPXPRM00
ROOT FILESYSTEM('OMVS.ROOT.ZFS')
     TYPE(ZFS) MODE(RDWR)
MOUNT FILESYSTEM('OMVS.ETC.ZFS')
      MOUNTPOINT('/etc')
      TYPE(ZFS) MODE(RDWR)
MAXPROCSYS(3000)          /* max processes per system */
TZ(EST5EDT)
