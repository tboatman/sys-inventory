---
- name: Dump the APF-authorized library list (D PROG,APF)
  ansible.builtin.command: >
    python3 {{ zos_extract_remote_script_dir }}/extrapf.py
    --outfile {{ zos_extract_remote_output_dir }}/apf.txt
  environment: "{{ zos_extract_env }}"
  changed_when: true
